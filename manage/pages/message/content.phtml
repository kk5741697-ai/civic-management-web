<?php
    if (check_permission('message') || Wo_IsAdmin() || Wo_IsModerator()) {
        
        $tab = (isset($_GET['tab']) && !empty($_GET['tab'])) ? $_GET['tab'] : 'whatsapp';
        echo Wo_LoadManagePage('message/' . $tab);
    } else {
        echo Wo_LoadManagePage('permission-required/content');
    }
?>
