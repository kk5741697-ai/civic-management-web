<!-- Financial Summary -->
<div class="card border-0 shadow-sm mb-3">
  <div class="card-body p-3">
    <h6 class="mb-3">Financial Summary</h6>
    
    <div class="d-flex justify-content-between align-items-center mb-2">
      <span class="text-muted">Total Amount</span>
      <span class="fw-semibold">৳<?php echo number_format($total_amount, 2); ?></span>
    </div>
    
    <div class="d-flex justify-content-between align-items-center mb-2">
      <span class="text-muted">Total Paid</span>
      <span class="fw-semibold text-success">৳<?php echo number_format($total_paid, 2); ?></span>
    </div>
    
    <div class="d-flex justify-content-between align-items-center mb-3">
      <span class="text-muted">Total Due</span>
      <span class="fw-semibold <?php echo $total_due > 0 ? 'text-danger' : 'text-success'; ?>">
        ৳<?php echo number_format($total_due, 2); ?>
      </span>
    </div>

    <?php if ($total_due > 0): ?>
    <button class="btn btn-primary btn-sm w-100" onclick="createInvoice(<?php echo $client['id']; ?>)">
      Create Invoice
    </button>
    <?php endif; ?>
  </div>
</div>

<!-- Payment History -->
<?php if (!empty($invoice)): ?>
<div class="card border-0 shadow-sm">
  <div class="card-body p-3">
    <h6 class="mb-3">Recent Payments</h6>
    
    <div class="d-grid gap-2">
      <?php foreach (array_slice($invoice, 0, 5) as $payment): ?>
      <div class="d-flex justify-content-between align-items-center py-2 border-bottom">
        <div>
          <div class="fw-medium">৳<?php echo number_format($payment['pay_amount'], 2); ?></div>
          <div class="small text-muted"><?php echo htmlspecialchars($payment['pay_type']); ?></div>
        </div>
        <div class="text-end">
          <div class="small"><?php echo date('d M Y', strtotime($payment['inv_time'])); ?></div>
          <div class="small text-muted">Invoice #<?php echo $payment['inv_id']; ?></div>
        </div>
      </div>
      <?php endforeach; ?>
    </div>

    <?php if (count($invoice) > 5): ?>
    <div class="text-center mt-2">
      <button class="btn btn-outline-secondary btn-sm" onclick="viewAllPayments(<?php echo $client['id']; ?>)">
        View All Payments
      </button>
    </div>
    <?php endif; ?>

  </div>
</div>
<?php endif; ?>