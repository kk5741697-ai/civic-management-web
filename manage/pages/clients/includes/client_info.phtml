<!-- Client Information Card -->
<div class="card border-0 shadow-sm mb-3">
  <div class="card-body p-3">
    <div class="d-flex align-items-center justify-content-between mb-3">
      <h6 class="mb-0">Client Information</h6>
      <div class="small text-muted">Created: <?php echo date('d M Y', strtotime($client['time'])); ?></div>
    </div>

    <div class="row gx-3 gy-2">
      <div class="col-6 col-md-4">
        <div class="small text-muted">Full Name</div>
        <div class="fw-medium"><?php echo htmlspecialchars($client['name']); ?></div>
      </div>

      <?php if (!empty($additional['spouse_name'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Spouse Name</div>
        <div class="fw-medium"><?php echo htmlspecialchars($additional['spouse_name']); ?></div>
      </div>
      <?php endif; ?>

      <?php if (!empty($additional['fathers_name'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Father's Name</div>
        <div class="fw-medium"><?php echo htmlspecialchars($additional['fathers_name']); ?></div>
      </div>
      <?php endif; ?>

      <?php if (!empty($additional['mothers_name'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Mother's Name</div>
        <div class="fw-medium"><?php echo htmlspecialchars($additional['mothers_name']); ?></div>
      </div>
      <?php endif; ?>

      <div class="col-6 col-md-4">
        <div class="small text-muted">Phone</div>
        <div class="fw-medium">
          <a href="tel:<?php echo htmlspecialchars($client['phone']); ?>" class="text-decoration-none">
            <?php echo htmlspecialchars($client['phone']); ?>
          </a>
        </div>
      </div>

      <?php if (!empty($additional['email'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Email</div>
        <div class="fw-medium">
          <a href="mailto:<?php echo htmlspecialchars($additional['email']); ?>" class="text-decoration-none">
            <?php echo htmlspecialchars($additional['email']); ?>
          </a>
        </div>
      </div>
      <?php endif; ?>

      <div class="col-12">
        <div class="small text-muted">Present Address</div>
        <div class="fw-medium"><?php echo htmlspecialchars($client['address']); ?></div>
      </div>

      <?php if (!empty($additional['permanent_addr'])): ?>
      <div class="col-12">
        <div class="small text-muted">Permanent Address</div>
        <div class="fw-medium"><?php echo htmlspecialchars($additional['permanent_addr']); ?></div>
      </div>
      <?php endif; ?>

      <?php if (!empty($additional['birthday'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Date of Birth</div>
        <div class="fw-medium"><?php echo date('d M Y', strtotime($additional['birthday'])); ?></div>
      </div>
      <?php endif; ?>

      <?php if (!empty($additional['nid'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">National ID</div>
        <div class="fw-medium"><?php echo htmlspecialchars($additional['nid']); ?></div>
      </div>
      <?php endif; ?>

      <?php if (!empty($additional['nationality'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Nationality</div>
        <div class="fw-medium"><?php echo htmlspecialchars($additional['nationality']); ?></div>
      </div>
      <?php endif; ?>

      <?php if (!empty($additional['religion'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Religion</div>
        <div class="fw-medium"><?php echo htmlspecialchars($additional['religion']); ?></div>
      </div>
      <?php endif; ?>

      <?php if (!empty($additional['profession'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Profession</div>
        <div class="fw-medium"><?php echo htmlspecialchars($additional['profession']); ?></div>
      </div>
      <?php endif; ?>

      <?php if (!empty($additional['reference'])): ?>
      <div class="col-6 col-md-4">
        <div class="small text-muted">Reference</div>
        <div class="fw-medium">
          <?php 
          $ref_user = Wo_UserData($additional['reference']);
          echo htmlspecialchars($ref_user['name'] ?? 'Unknown');
          ?>
        </div>
      </div>
      <?php endif; ?>
    </div>

    <!-- Nominees Section -->
    <?php if (!empty($nominees)): ?>
    <div class="mt-3 pt-3 border-top">
      <div class="small text-muted mb-2">Nominees</div>
      <div class="d-flex flex-wrap gap-2">
        <?php foreach ($nominees as $nominee): ?>
          <div class="badge bg-light text-dark border">
            <?php echo htmlspecialchars($nominee->name); ?>
            <?php if (!empty($nominee->relation)): ?>
              (<?php echo htmlspecialchars($nominee->relation); ?>)
            <?php endif; ?>
            <?php if (!empty($nominee->share_parcent)): ?>
              - <?php echo htmlspecialchars($nominee->share_parcent); ?>%
            <?php endif; ?>
          </div>
        <?php endforeach; ?>
      </div>
    </div>
    <?php endif; ?>

  </div>
</div>