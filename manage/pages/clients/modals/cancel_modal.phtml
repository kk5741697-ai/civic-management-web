<!-- Cancel Purchase Modal -->
<div class="modal fade" id="cancelPurchaseModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header bg-light">
        <h5 class="modal-title text-danger">Cancel Purchase</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      
      <form id="cancelPurchaseForm">
        <div class="modal-body">
          <input type="hidden" name="booking_helper_id" id="cancelPurchaseId">
          
          <div class="alert alert-warning">
            <i class="bx bx-warning"></i>
            <strong>Warning:</strong> This action will cancel the purchase and make the plot available again.
          </div>

          <div class="mb-3">
            <label class="form-label">Cancellation Date</label>
            <input type="date" name="cancel_date" id="cancelDate" class="form-control" value="<?php echo date('Y-m-d'); ?>" required>
          </div>

          <div class="mb-3">
            <label class="form-label">Reason for Cancellation</label>
            <textarea name="cancel_reason" id="cancelReason" class="form-control" rows="3" placeholder="Please provide a reason for cancellation..." required></textarea>
          </div>

          <div id="cancelAlert"></div>
        </div>

        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
          <button type="submit" class="btn btn-danger">Cancel Purchase</button>
        </div>
      </form>
    </div>
  </div>
</div>